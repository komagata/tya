import serializer

class Player
  initialize: name, score ->
    self.name = name
    self.score = score

player = Player("Ada", 42)
saved_json = serializer.Serializer(nil).to_json(player, { include_class: true })
println(saved_json)

loaded = serializer.Serializer(nil).from_json(saved_json, Player)
println(loaded.name)
println(loaded.score)
