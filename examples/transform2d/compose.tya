import geometry/*
import transform2_d as transform2d

move = Transform2D(1, 0, 0, 1, 0, 0).translation(10, 20)
scale = Transform2D(1, 0, 0, 1, 0, 0).uniform_scale(2)
world = move.compose(scale)

point = world.apply_point(geometry.Point(3, 4))
print(point.to_vector().to_array())
