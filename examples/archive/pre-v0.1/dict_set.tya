user = { name: "komagata", age: 20 }
print user["name"]

empty = {}
print empty.len()

roles = { "admin", "owner", "admin" }
print roles.len()
print roles.has("owner")

empty_roles = set()
print empty_roles.len()
