import collections as collections

queue = collections.Queue(nil)
queue.push("assets")
queue.push("assets/sprites")
queue.push("assets/audio")

visited = collections.Set(nil)

while not queue.empty?()
  path = queue.pop()
  if not visited.has?(path)
    visited.add(path)
    print(path)
