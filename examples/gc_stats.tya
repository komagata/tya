import runtime

before = runtime.Runtime().gc_stats()

items = []
i = 0
while i < 100
  items.push([i, i + 1, i + 2])
  i = i + 1

after = runtime.Runtime().gc_stats()

print("before alloc_count: {before[\"alloc_count\"]}")
print("after  alloc_count: {after[\"alloc_count\"]}")
print("live_count after: {after[\"live_count\"]}")
