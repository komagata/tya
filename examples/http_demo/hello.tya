# v0.58: minimal HTTP server.
# Run: tya run examples/http_demo/hello.tya
# Then in another shell: curl http://localhost:8080/
import net/http/*

app = net.http.Server()
app.get("/", _req -> { status: 200, body: "Hello, Tya HTTP" })
app.run(8080)
