interface Comparable
  compare = other ->

  lt? = other ->
    self.compare(other) < 0

  lte? = other ->
    self.compare(other) <= 0

  gt? = other ->
    self.compare(other) > 0

  gte? = other ->
    self.compare(other) >= 0

  between? = min, max ->
    self.gte?(min) and self.lte?(max)
