class Time
  static now = () -> time_now()

  static sleep = seconds -> time_sleep(seconds)

  static format = t, layout -> time_format(t, layout)

  static parse = text -> time_parse(text)

  static since = t -> time_since(t)
