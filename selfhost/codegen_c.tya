kindOf = node ->
  parts = split node, ":"
  parts[0]

nameOf = node ->
  parts = split node, ":"
  if len(parts) < 2
    return ""
  parts[1]

emitC = nodes ->
  lines = []
  push lines, "#include <stdio.h>"
  push lines, ""
  push lines, "int main(void) {"

  for node in nodes
    if kindOf(node) == "ASSIGN"
      push lines, "  /* assign " + nameOf(node) + " */"
    if kindOf(node) == "PRINT"
      push lines, "  puts(\"" + nameOf(node) + "\");"

  push lines, "  return 0;"
  push lines, "}"
  join lines, "\n"

nodes = []
source = readFile(args()[0])
lines = split source, "\n"

for line in lines
  if line != ""
    push nodes, line

print emitC nodes
