# v0.57 a glob pattern that matches zero files is a TYA-E0611
# error (caught at codegen, so `tya emit-c` or `tya run` surfaces
# it).
cd $REPO
! exec go run ./cmd/tya emit-c $WORK/main.tya
stderr 'TYA-E0611'

-- assets/data.txt --
present
-- main.tya --
embed "assets/*.svg" as none
print(none)
