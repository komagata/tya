# v0.50 `tya new --no-git` skips the default git init step. The
# scaffold lands but .git/ is not created.
cd $REPO
exec go build -o $WORK/tya ./cmd/tya
cd $WORK

exec $WORK/tya new --no-git myapp
stdout 'Created myapp'
! stderr .

exists myapp/tya.toml
! exists myapp/.git
