# v0.50 `tya new <name>` runs `git init` by default. The scaffold
# has a fully initialised .git/ directory.
cd $REPO
exec go build -o $WORK/tya ./cmd/tya
cd $WORK

exec $WORK/tya new myapp
stdout 'Created myapp'
! stderr .

exists myapp/.git
exists myapp/.git/HEAD
