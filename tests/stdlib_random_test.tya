import unittest
import random

module stdlib_random_test
  test_seed_reproducible = ->
    random.seed(42)
    a = random.int(1, 1000)
    random.seed(42)
    b = random.int(1, 1000)
    unittest.assert_equal(a, b, "seeded reproducibility")
  test_int_range = ->
    random.seed(1)
    i = 0
    while i < 50
      v = random.int(0, 9)
      unittest.assert(v >= 0, "lower")
      unittest.assert(v <= 9, "upper")
      i = i + 1
  test_float_range = ->
    f = random.float()
    unittest.assert(f >= 0, "f >= 0")
    unittest.assert(f < 1, "f < 1")
  test_choice_picks_member = ->
    items = ["a", "b", "c"]
    c = random.choice(items)
    unittest.assert(c == "a" or c == "b" or c == "c", "valid choice")
