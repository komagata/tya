import unittest
import file as file

class StdlibFileTest extends TestCase

  test_write_then_read = ->
    p = "/tmp/tya_v022_test.txt"
    file.File.write(p, "hello tya")
    self.assert_equal("hello tya", file.File.read(p), "round-trip")
  test_exists_true = ->
    p = "/tmp/tya_v022_test.txt"
    file.File.write(p, "hi")
    self.assert(file.File.exists?(p), "exists")
  test_exists_false = ->
    self.assert_equal(false, file.File.exists?("/tmp/__tya_v022_no_such__"), "missing")
